#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - compute nu,mber od characters in string.
 * @s: String param.
 * Return: Integer (string length).
 */
int _strlen(char *s)
{
	int len = strlen(s);
	return len;
}
